import { Classes.QuestionPlayable } from './classes.question-playable';

describe('Classes.QuestionPlayable', () => {
  it('should create an instance', () => {
    expect(new Classes.QuestionPlayable()).toBeTruthy();
  });
});
