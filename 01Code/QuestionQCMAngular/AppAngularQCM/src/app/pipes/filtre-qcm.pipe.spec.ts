import { FiltreQCMPipe } from './filtre-qcm.pipe';

describe('FiltreQCMPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreQCMPipe();
    expect(pipe).toBeTruthy();
  });
});
